# 위치공유(myWhere) 릴레이 — Cloudflare Worker 설정
name = "mywhere-relay"
main = "worker.js"
compatibility_date = "2024-11-01"

# KV 네임스페이스 바인딩 (아래 id는 `wrangler kv namespace create MYWHERE` 실행 후 출력값으로 교체)
[[kv_namespaces]]
binding = "MYWHERE"
id = "여기에_KV_네임스페이스_ID_붙여넣기"
